---
layout: layouts/base.njk
section: posts
---

<section class="card" id="posts">
	<h1>{{ title }}</h1>
  <p>I write about all sorts of things here, use the tags below to narrow down your search or browse through everything in order.</p>
  <div class="list tag-list">
    <h2>Filter by Tag:</h2>
    {% for tag in collections.tagList | filterTagList %}
      {% set tagUrl %}/tags/{{ tag | slug }}/{% endset %}
      <a href="{{ tagUrl | url }}" class="post-tag listify text-capitalize">{{ tag }}</a>
    {% endfor %}
  </div>
  
</section>
{% set postslist = collections.post %}
{% include "components/partials/post-grid.njk" %}