---
import Base from "../layouts/Base.astro";
---

<Base wide>
  <style>
    .demo-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      grid-auto-rows: 200px;
      gap: var(--sp-2);
      padding: var(--sp-3);
    }
    .demo-section {
      grid-column: 1 / -1;
      padding: var(--sp-3) 0 var(--sp);
      border-bottom: 1px solid var(--border);
      height: auto;
    }
    .demo-section h2 {
      font-size: var(--fs-l);
      color: var(--fg);
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }
    .demo-section p {
      font-size: var(--fs-s);
      color: var(--fg-2);
      margin-top: var(--sp);
    }
    .demo-w { grid-column: span 2; }
    .demo-tall { grid-row: span 2; }
    .demo-hero { grid-column: span 3; grid-row: span 2; }

    /* ========================================
       POSTS
       ======================================== */

    /* --- P1: Dossier Folder --- */
    .p1-dossier {
      position: relative;
      overflow: hidden;
      background: var(--surface-2);
      clip-path: polygon(
        0 10px,
        20% 10px, 22% 0, 42% 0, 44% 10px,
        100% 10px,
        100% calc(100% - 16px),
        calc(100% - 16px) 100%,
        0 100%
      );
      padding: 20px var(--sp-3) var(--sp-3);
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      gap: var(--sp);
      background-image: repeating-linear-gradient(
        0deg, transparent, transparent 19px,
        color-mix(in oklch, var(--accent) 8%, transparent) 19px,
        color-mix(in oklch, var(--accent) 8%, transparent) 20px
      );
      background-color: var(--surface-2);
    }
    .p1-dossier::after {
      content: '';
      position: absolute;
      bottom: 0; right: 0;
      width: 16px; height: 16px;
      background: linear-gradient(135deg, var(--surface) 50%, var(--bg) 50%);
    }
    .p1-stamp {
      position: absolute;
      top: 18px; right: 12px;
      font-family: var(--mono);
      font-size: 48px; font-weight: 700;
      color: var(--accent); opacity: 0.12;
      transform: rotate(12deg);
      line-height: 1;
    }
    .p1-tab {
      font-family: var(--mono);
      font-size: var(--fs-s);
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--accent);
    }

    /* --- P2: Circuit Board (standalone) --- */
    .p2-circuit {
      position: relative;
      overflow: hidden;
      clip-path: polygon(
        0 0, 100% 0,
        100% 25%, calc(100% - 4px) 25%, calc(100% - 4px) 35%, 100% 35%,
        100% 55%, calc(100% - 4px) 55%, calc(100% - 4px) 65%, 100% 65%,
        100% 100%, 0 100%,
        0 65%, 4px 65%, 4px 55%, 0 55%,
        0 35%, 4px 35%, 4px 25%, 0 25%
      );
      padding: var(--sp-3);
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      gap: var(--sp);
      background:
        repeating-linear-gradient(0deg, transparent, transparent 19px, color-mix(in oklch, var(--accent) 5%, transparent) 19px, color-mix(in oklch, var(--accent) 5%, transparent) 20px),
        repeating-linear-gradient(90deg, transparent, transparent 19px, color-mix(in oklch, var(--accent) 5%, transparent) 19px, color-mix(in oklch, var(--accent) 5%, transparent) 20px),
        color-mix(in oklch, var(--accent) 3%, var(--surface));
    }
    .p2-circuit::before {
      content: '';
      position: absolute; inset: 0;
      background:
        linear-gradient(90deg, transparent 20%, color-mix(in oklch, var(--accent) 12%, transparent) 20%, color-mix(in oklch, var(--accent) 12%, transparent) 20.5%, transparent 20.5%),
        linear-gradient(0deg, transparent 40%, color-mix(in oklch, var(--accent) 12%, transparent) 40%, color-mix(in oklch, var(--accent) 12%, transparent) 40.5%, transparent 40.5%),
        linear-gradient(90deg, transparent 60%, color-mix(in oklch, var(--accent) 8%, transparent) 60%, color-mix(in oklch, var(--accent) 8%, transparent) 60.5%, transparent 60.5%);
      pointer-events: none;
    }
    .p2-chip {
      border: 1px solid color-mix(in oklch, var(--accent) 20%, transparent);
      padding: var(--sp-2);
      position: relative; z-index: 1;
    }
    .p2-part {
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2);
      letter-spacing: 0.05em;
    }

    /* --- P3: Blast Door --- */
    .p3-blast {
      position: relative;
      overflow: hidden;
      background: var(--surface-2);
      clip-path: polygon(8px 0, calc(100% - 8px) 0, 100% 8px, 100% 100%, 0 100%, 0 8px);
      padding: 0;
      display: flex;
      flex-direction: column;
    }
    .p3-blast::after {
      content: '';
      position: absolute; inset: 0;
      box-shadow: inset 2px 2px 0 rgba(255,255,255,0.04), inset -2px -2px 0 rgba(0,0,0,0.3);
      pointer-events: none; z-index: 2;
    }
    .p3-rail {
      display: flex;
      align-items: center;
      gap: var(--sp-2);
      padding: var(--sp) var(--sp-3);
      padding-left: calc(var(--sp-3) + 4px);
      background: linear-gradient(180deg, var(--surface-2), var(--surface), var(--surface-2));
      border-bottom: 2px solid var(--border);
      flex-shrink: 0;
    }
    .p3-hex {
      width: 7px; height: 8px;
      background: color-mix(in oklch, var(--fg-2) 40%, var(--surface));
      clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
    }
    .p3-label {
      font-family: var(--mono);
      font-size: var(--fs-s);
      text-transform: uppercase;
      letter-spacing: 0.15em;
      color: var(--fg-2);
    }
    .p3-body {
      flex: 1;
      padding: var(--sp-3);
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      gap: var(--sp);
      position: relative; z-index: 1;
    }
    .p3-stripe {
      height: 4px;
      background: repeating-linear-gradient(135deg,
        color-mix(in oklch, var(--accent) 20%, transparent) 0px,
        color-mix(in oklch, var(--accent) 20%, transparent) 6px,
        transparent 6px, transparent 12px
      );
      flex-shrink: 0;
    }

    /* --- P4: Data Cassette (original) --- */
    .p4-cassette {
      position: relative;
      overflow: hidden;
      background: var(--surface-2);
      clip-path: polygon(
        4px 0, calc(100% - 4px) 0,
        calc(100% - 2px) 6px, 100% 6px,
        100% calc(100% - 3px), calc(100% - 3px) 100%,
        3px 100%, 0 calc(100% - 3px),
        0 6px, 2px 6px
      );
      padding: 0;
      display: flex;
      flex-direction: column;
      background-image: repeating-linear-gradient(0deg, transparent, transparent 3px, rgba(255,255,255,0.008) 3px, rgba(255,255,255,0.008) 4px);
      background-color: var(--surface-2);
    }
    .p4-label {
      margin: 8px 10px 0;
      border: 1px solid var(--border);
      background: color-mix(in oklch, var(--surface) 50%, var(--bg));
      padding: var(--sp-2) var(--sp-3);
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      gap: var(--sp);
    }
    .p4-catalog {
      position: absolute;
      top: 14px; right: 18px;
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2); opacity: 0.6;
    }
    .p4-window {
      margin: var(--sp-2) 10px 10px;
      height: 8px;
      background: var(--bg);
      border: 1px solid var(--border);
      box-shadow: inset 0 0 6px color-mix(in oklch, var(--accent) 15%, transparent);
      flex-shrink: 0;
    }

    /* --- PX: Cassette + Circuit Hybrid --- */
    .px-hybrid {
      position: relative;
      overflow: hidden;
      background: var(--surface-2);
      border: 1px solid var(--border);
      /* Pin notches flanking the slim circuit edge */
      clip-path: polygon(
        4px 0, calc(100% - 4px) 0,
        calc(100% - 2px) 6px, 100% 6px,
        100% 86%, calc(100% - 6px) 86%, calc(100% - 6px) 91%, 100% 91%,
        100% calc(100% - 3px), calc(100% - 3px) 100%,
        3px 100%, 0 calc(100% - 3px),
        0 91%, 6px 91%, 6px 86%, 0 86%,
        0 6px, 2px 6px
      );
      padding: 0;
      display: flex;
      flex-direction: column;
      background-image: repeating-linear-gradient(0deg, transparent, transparent 3px, rgba(255,255,255,0.008) 3px, rgba(255,255,255,0.008) 4px);
      background-color: var(--surface-2);
    }
    .px-label {
      margin: 8px 10px 0;
      border: 1px solid var(--border);
      background: color-mix(in oklch, var(--surface) 50%, var(--bg));
      padding: var(--sp-2) var(--sp-3);
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      gap: var(--sp);
    }
    .px-catalog {
      position: absolute;
      top: 14px; right: 18px;
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2); opacity: 0.6;
    }
    /* Circuit board insertion edge — slim strip */
    .px-edge {
      margin: 0;
      height: 24px;
      flex-shrink: 0;
      position: relative;
      border-top: 1px solid color-mix(in oklch, var(--accent) 35%, var(--border));
      /* Sparse PCB grid — wide spacing, subtle */
      background:
        repeating-linear-gradient(0deg, transparent, transparent 23px, color-mix(in oklch, var(--accent) 8%, transparent) 23px, color-mix(in oklch, var(--accent) 8%, transparent) 24px),
        repeating-linear-gradient(90deg, transparent, transparent 23px, color-mix(in oklch, var(--accent) 8%, transparent) 23px, color-mix(in oklch, var(--accent) 8%, transparent) 24px),
        color-mix(in oklch, var(--accent) 3%, var(--bg));
    }
    /* A couple of trace lines — thin, understated */
    .px-edge::before {
      content: '';
      position: absolute; inset: 0;
      background:
        linear-gradient(90deg, transparent 22%, color-mix(in oklch, var(--accent) 18%, transparent) 22%, color-mix(in oklch, var(--accent) 18%, transparent) 22.5%, transparent 22.5%),
        linear-gradient(90deg, transparent 65%, color-mix(in oklch, var(--accent) 18%, transparent) 65%, color-mix(in oklch, var(--accent) 18%, transparent) 65.5%, transparent 65.5%);
      pointer-events: none;
    }
    /* Solder pads — small dots along bottom */
    .px-edge::after {
      content: '';
      position: absolute;
      bottom: 4px; left: 12px; right: 12px;
      height: 2px;
      background: repeating-linear-gradient(90deg,
        color-mix(in oklch, var(--accent) 25%, transparent) 0px,
        color-mix(in oklch, var(--accent) 25%, transparent) 3px,
        transparent 3px, transparent 18px
      );
    }

    /* ========================================
       TYPOGRAPHIC POSTS
       ======================================== */

    /* --- P8: Cassette + Slab hybrid --- */
    .p8-casslab {
      position: relative;
      overflow: hidden;
      background: var(--surface-2);
      border: 1px solid var(--border);
      clip-path: polygon(
        4px 0, calc(100% - 4px) 0,
        calc(100% - 2px) 6px, 100% 6px,
        100% 88%, calc(100% - 6px) 88%, calc(100% - 6px) 92%, 100% 92%,
        100% calc(100% - 3px), calc(100% - 3px) 100%,
        3px 100%, 0 calc(100% - 3px),
        0 92%, 6px 92%, 6px 88%, 0 88%,
        0 6px, 2px 6px
      );
      padding: 0;
      display: flex;
      flex-direction: column;
      background-image: repeating-linear-gradient(0deg, transparent, transparent 3px, rgba(255,255,255,0.008) 3px, rgba(255,255,255,0.008) 4px);
      background-color: var(--surface-2);
    }
    .p8-catalog {
      position: absolute;
      top: 12px; right: 16px;
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2); opacity: 0.5;
      z-index: 2;
    }
    /* Main label area — title dominates */
    .p8-label {
      margin: 8px 10px 0;
      border: 1px solid var(--border);
      background: color-mix(in oklch, var(--surface) 50%, var(--bg));
      padding: var(--sp-2) var(--sp-3);
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      gap: var(--sp);
      overflow: hidden;
    }
    /* Big title — scales with container, clips cleanly */
    .p8-big {
      font-family: var(--font);
      font-size: clamp(24px, 5cqi, 48px);
      font-weight: 800;
      line-height: 0.9;
      letter-spacing: -0.02em;
      text-transform: uppercase;
      color: var(--fg);
      overflow: hidden;
      display: -webkit-box;
      -webkit-line-clamp: 4;
      -webkit-box-orient: vertical;
    }
    .demo-w .p8-big {
      font-size: clamp(28px, 6cqi, 56px);
      -webkit-line-clamp: 3;
    }
    .demo-tall .p8-big {
      font-size: clamp(32px, 7cqi, 64px);
      -webkit-line-clamp: 5;
    }
    .p8-sub {
      font-size: var(--fs-s);
      color: var(--fg-2);
      line-height: var(--lh);
      overflow: hidden;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
    }
    /* Footer — type label + date, sits between label and edge */
    .p8-footer {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      padding: var(--sp) 10px;
    }
    .p8-type {
      font-family: var(--mono);
      font-size: var(--fs-s);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--accent);
    }
    .p8-date {
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2);
    }
    /* Circuit edge — slim strip at bottom */
    .p8-edge {
      height: 20px;
      flex-shrink: 0;
      position: relative;
      border-top: 1px solid color-mix(in oklch, var(--accent) 35%, var(--border));
      background:
        repeating-linear-gradient(0deg, transparent, transparent 19px, color-mix(in oklch, var(--accent) 8%, transparent) 19px, color-mix(in oklch, var(--accent) 8%, transparent) 20px),
        repeating-linear-gradient(90deg, transparent, transparent 19px, color-mix(in oklch, var(--accent) 8%, transparent) 19px, color-mix(in oklch, var(--accent) 8%, transparent) 20px),
        color-mix(in oklch, var(--accent) 3%, var(--bg));
    }
    .p8-edge::before {
      content: '';
      position: absolute; inset: 0;
      background:
        linear-gradient(90deg, transparent 25%, color-mix(in oklch, var(--accent) 18%, transparent) 25%, color-mix(in oklch, var(--accent) 18%, transparent) 25.5%, transparent 25.5%),
        linear-gradient(90deg, transparent 60%, color-mix(in oklch, var(--accent) 18%, transparent) 60%, color-mix(in oklch, var(--accent) 18%, transparent) 60.5%, transparent 60.5%);
      pointer-events: none;
    }
    .p8-edge::after {
      content: '';
      position: absolute;
      bottom: 3px; left: 12px; right: 12px;
      height: 2px;
      background: repeating-linear-gradient(90deg,
        color-mix(in oklch, var(--accent) 25%, transparent) 0px,
        color-mix(in oklch, var(--accent) 25%, transparent) 3px,
        transparent 3px, transparent 18px
      );
    }

    /* --- P7: Departure Board — split-flap arrivals aesthetic --- */
    .p7-board {
      position: relative;
      overflow: hidden;
      background: #111318;
      padding: 0;
      display: flex;
      flex-direction: column;
      border: 2px solid #2a2d35;
      clip-path: none;
      border-radius: 3px;
    }
    .p7-row {
      display: flex;
      align-items: center;
      padding: 0;
      border-bottom: 1px solid #1e2028;
      flex-shrink: 0;
    }
    .p7-row-head {
      background: #1a1d25;
      border-bottom: 2px solid #2a2d35;
    }
    .p7-row-head .p7-col {
      font-size: 9px;
      color: #4a5068;
      letter-spacing: 0.15em;
      padding: 5px 8px;
    }
    .p7-row-active {
      background: #181b22;
    }
    .p7-row-active .p7-col-dest {
      color: var(--accent);
    }
    .p7-row-dim {
      opacity: 0.4;
    }
    .p7-col {
      font-family: var(--mono);
      font-size: var(--fs);
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: var(--fg);
      padding: 8px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      /* Split-flap cell look */
      background: linear-gradient(
        180deg,
        transparent 48%,
        rgba(0,0,0,0.15) 48%,
        rgba(0,0,0,0.15) 52%,
        transparent 52%
      );
    }
    .p7-col-id {
      width: 36px;
      flex-shrink: 0;
      text-align: center;
      color: var(--fg-2);
      border-right: 1px solid #1e2028;
    }
    .p7-col-dest {
      flex: 1;
      min-width: 0;
    }
    .p7-col-time {
      width: 52px;
      flex-shrink: 0;
      text-align: right;
      color: var(--fg-2);
      border-left: 1px solid #1e2028;
    }
    .p7-col-status {
      width: 48px;
      flex-shrink: 0;
      text-align: center;
      font-size: var(--fs-s);
      color: var(--fg-2);
      border-left: 1px solid #1e2028;
    }
    .p7-live {
      color: var(--accent);
    }
    /* Ambient glow at top */
    .p7-board::after {
      content: '';
      position: absolute;
      top: 0; left: 10%; right: 10%;
      height: 1px;
      background: linear-gradient(90deg,
        transparent,
        color-mix(in oklch, var(--accent) 30%, transparent) 30%,
        color-mix(in oklch, var(--accent) 50%, transparent) 50%,
        color-mix(in oklch, var(--accent) 30%, transparent) 70%,
        transparent
      );
      z-index: 1;
    }

    /* ========================================
       LINK VARIANT B — relay dispatch
       ======================================== */

    .lB-dispatch {
      position: relative;
      overflow: hidden;
      background: var(--bg);
      padding: 0;
      display: flex;
      flex-direction: column;
      color: var(--fg);
      grid-column: span 2;
      /* Stepped corners — mirrored from relay A */
      clip-path: polygon(
        0 0,
        calc(100% - 14px) 0,
        calc(100% - 14px) 14px,
        100% 14px,
        100% 100%,
        14px 100%,
        14px calc(100% - 14px),
        0 calc(100% - 14px)
      );
      border: 1px solid color-mix(in oklch, var(--link-accent) 35%, var(--border));
    }
    .lB-dispatch:hover {
      border-color: var(--link-accent);
    }
    /* Inner frame following stepped shape */
    .lB-dispatch::after {
      content: '';
      position: absolute;
      inset: 3px;
      border: 1px solid color-mix(in oklch, var(--link-accent) 12%, transparent);
      clip-path: polygon(
        0 0,
        calc(100% - 11px) 0,
        calc(100% - 11px) 11px,
        100% 11px,
        100% 100%,
        11px 100%,
        11px calc(100% - 11px),
        0 calc(100% - 11px)
      );
      pointer-events: none;
      z-index: 3;
    }
    /* Thin accent top stripe */
    .lB-stripe {
      height: 3px;
      background: linear-gradient(90deg, var(--link-accent), color-mix(in oklch, var(--link-accent) 30%, transparent));
      flex-shrink: 0;
    }
    .lB-head {
      display: flex;
      align-items: baseline;
      gap: var(--sp-2);
      padding: var(--sp) var(--sp-3);
      padding-right: calc(var(--sp-3) + 8px);
      flex-shrink: 0;
      position: relative;
      z-index: 1;
    }
    .lB-arrow {
      color: var(--link-accent);
      font-weight: 700;
      font-size: var(--fs);
      line-height: 1;
    }
    .lB-domain {
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--link-accent);
      opacity: 0.8;
    }
    .lB-date {
      margin-left: auto;
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2);
    }
    .lB-body {
      flex: 1;
      padding: 0 var(--sp-3) var(--sp-3);
      padding-right: calc(var(--sp-3) + 8px);
      display: flex;
      flex-direction: column;
      gap: var(--sp);
      position: relative;
      z-index: 1;
    }
    .lB-body h3 {
      font-size: var(--fs);
      font-weight: 600;
      text-transform: none;
      letter-spacing: 0;
      line-height: var(--lh);
      color: var(--fg);
    }
    .lB-comment {
      font-size: var(--fs-s);
      color: var(--fg-2);
      line-height: var(--lh);
      font-style: italic;
    }

    /* ========================================
       SHOTS — 3 variants that rotate
       ======================================== */

    .shot-img {
      width: 100%; height: 100%;
      object-fit: cover;
      background: linear-gradient(135deg, var(--surface), var(--surface-2));
    }

    /* --- S1: Viewfinder Frame --- */
    .s1-viewfinder {
      position: relative;
      overflow: hidden;
      clip-path: polygon(10px 0, 100% 0, 100% calc(100% - 10px), calc(100% - 10px) 100%, 0 100%, 0 10px);
      background: var(--surface);
    }
    .s1-viewfinder::before {
      content: '';
      position: absolute; inset: 0;
      background: color-mix(in oklch, var(--accent-2) 6%, transparent);
      pointer-events: none; z-index: 1;
    }
    .s1-viewfinder::after {
      content: '';
      position: absolute; inset: 0;
      background:
        linear-gradient(90deg, transparent calc(50% - 0.5px), color-mix(in oklch, var(--accent-2) 15%, transparent) calc(50% - 0.5px), color-mix(in oklch, var(--accent-2) 15%, transparent) calc(50% + 0.5px), transparent calc(50% + 0.5px)),
        linear-gradient(0deg, transparent calc(50% - 0.5px), color-mix(in oklch, var(--accent-2) 15%, transparent) calc(50% - 0.5px), color-mix(in oklch, var(--accent-2) 15%, transparent) calc(50% + 0.5px), transparent calc(50% + 0.5px));
      pointer-events: none; z-index: 2;
    }
    .s1-exposure {
      position: absolute;
      bottom: 8px; left: 10px;
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--accent-2); z-index: 3;
      text-shadow: 0 1px 3px rgba(0,0,0,0.8);
    }
    .s1-ticks {
      position: absolute;
      top: 0; right: 0;
      height: 100%; width: 12px; z-index: 3;
      background: repeating-linear-gradient(0deg,
        transparent, transparent 18px,
        color-mix(in oklch, var(--accent-2) 25%, transparent) 18px,
        color-mix(in oklch, var(--accent-2) 25%, transparent) 20px
      );
    }

    /* --- S2: Evidence Photo --- */
    .s2-evidence {
      position: relative;
      overflow: visible;
      clip-path: polygon(
        0 8px,
        15px 8px, 18px 0, 40px 0, 43px 8px,
        calc(100% - 43px) 8px, calc(100% - 40px) 0, calc(100% - 18px) 0, calc(100% - 15px) 8px,
        100% 8px,
        100% 100%, 0 100%
      );
      background: var(--surface);
      border: 1px solid rgba(255,255,255,0.06);
      box-shadow: 2px 3px 10px rgba(0,0,0,0.35);
      transition: transform 0.2s ease;
    }
    .s2-evidence:hover { transform: rotate(0.5deg) translateY(-2px); }
    .s2-evidence::before {
      content: '';
      position: absolute; top: 0; left: 18px;
      width: 22px; height: 10px;
      background: rgba(255,255,255,0.06); z-index: 2;
    }
    .s2-evidence::after {
      content: '';
      position: absolute; top: 0; right: 18px;
      width: 22px; height: 10px;
      background: rgba(255,255,255,0.06); z-index: 2;
    }
    .s2-marker {
      position: absolute;
      bottom: 10px; right: 10px;
      background: var(--accent-2);
      color: var(--bg);
      font-family: var(--mono);
      font-size: var(--fs); font-weight: 700;
      padding: 2px 8px;
      clip-path: polygon(0 0, 100% 0, 100% 80%, 50% 100%, 0 80%);
      z-index: 3; line-height: 1.3;
    }

    /* --- S4: HUD Capture --- */
    .s4-hud {
      position: relative;
      overflow: hidden;
      clip-path: polygon(4px 0, calc(100% - 4px) 0, 100% 100%, 0 100%);
      background: var(--surface);
    }
    .s4-hud > .shot-img { filter: sepia(0.05) hue-rotate(50deg); }
    .s4-hud::before {
      content: '';
      position: absolute; inset: 8px; z-index: 2;
      pointer-events: none;
      border: 1px solid color-mix(in oklch, var(--accent) 20%, transparent);
      -webkit-mask-image:
        linear-gradient(90deg, black 40%, transparent 40%, transparent 60%, black 60%),
        linear-gradient(0deg, black 40%, transparent 40%, transparent 60%, black 60%);
      -webkit-mask-composite: source-in;
      mask-image:
        linear-gradient(90deg, black 40%, transparent 40%, transparent 60%, black 60%),
        linear-gradient(0deg, black 40%, transparent 40%, transparent 60%, black 60%);
      mask-composite: intersect;
    }
    .s4-hud::after {
      content: '';
      position: absolute;
      left: 0; right: 0; top: 35%; height: 1px;
      background: linear-gradient(90deg, transparent, color-mix(in oklch, var(--accent) 25%, transparent) 20%, color-mix(in oklch, var(--accent) 40%, transparent) 50%, color-mix(in oklch, var(--accent) 25%, transparent) 80%, transparent);
      z-index: 2; pointer-events: none;
    }
    .s4-data {
      position: absolute;
      top: 10px; right: 14px;
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--accent); opacity: 0.6;
      z-index: 3;
      text-shadow: 0 1px 3px rgba(0,0,0,0.8);
      text-align: right; line-height: 1.5;
    }

    /* ========================================
       BSKY — 2 directions + hybrid
       ======================================== */

    .bsky-text-demo {
      font-size: var(--fs-s);
      line-height: var(--lh);
      color: var(--fg);
    }

    /* --- B2: Signal Packet --- */
    .b2-packet {
      position: relative;
      overflow: hidden;
      background: var(--surface-2);
      clip-path: polygon(
        4px 0, 100% 0,
        100% calc(100% - 4px), calc(100% - 4px) calc(100% - 4px), calc(100% - 4px) 100%,
        0 100%, 0 4px, 4px 4px
      );
      padding: 0;
      display: flex;
      flex-direction: column;
      border-left: 1px dashed color-mix(in oklch, #1185fe 30%, var(--border));
      border-right: 1px dashed color-mix(in oklch, #1185fe 30%, var(--border));
      border-top: 1px solid color-mix(in oklch, #1185fe 30%, var(--border));
      border-bottom: 1px solid color-mix(in oklch, #1185fe 30%, var(--border));
    }
    .b2-header {
      display: flex;
      padding: var(--sp) var(--sp-2);
      gap: var(--sp-2);
      background: color-mix(in oklch, #1185fe 4%, var(--surface));
      border-bottom: 1px solid color-mix(in oklch, #1185fe 15%, var(--border));
      flex-shrink: 0;
      font-family: var(--mono);
      font-size: var(--fs-s);
    }
    .b2-field {
      color: var(--fg-2);
      border-right: 1px solid color-mix(in oklch, #1185fe 15%, var(--border));
      padding-right: var(--sp-2);
    }
    .b2-field:last-child { border-right: none; }
    .b2-field em { color: #1185fe; font-style: normal; }
    .b2-payload {
      flex: 1;
      padding: var(--sp-2) var(--sp-3);
      display: flex;
      flex-direction: column;
      gap: var(--sp);
    }
    .b2-checksum {
      padding: var(--sp) var(--sp-2);
      background: color-mix(in oklch, #1185fe 3%, var(--surface));
      border-top: 1px solid color-mix(in oklch, #1185fe 12%, var(--border));
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2);
      flex-shrink: 0;
    }

    /* --- B4: Comm Ping --- */
    .b4-ping {
      position: relative;
      overflow: hidden;
      background: var(--surface-2);
      clip-path: polygon(0 0, calc(100% - 14px) 0, 100% 50%, calc(100% - 14px) 100%, 0 100%);
      padding: 0;
      display: flex;
      border: 1px solid color-mix(in oklch, #1185fe 25%, var(--border));
    }
    .b4-ping::before {
      content: '';
      position: absolute; inset: 0;
      background: radial-gradient(circle at 92% 50%,
        transparent 18px, color-mix(in oklch, #1185fe 18%, transparent) 19px, color-mix(in oklch, #1185fe 18%, transparent) 21px, transparent 22px,
        transparent 38px, color-mix(in oklch, #1185fe 12%, transparent) 39px, color-mix(in oklch, #1185fe 12%, transparent) 41px, transparent 42px,
        transparent 58px, color-mix(in oklch, #1185fe 7%, transparent) 59px, color-mix(in oklch, #1185fe 7%, transparent) 61px, transparent 62px
      );
      pointer-events: none; z-index: 1;
    }
    .b4-telem {
      width: 22px;
      flex-shrink: 0;
      background: color-mix(in oklch, #1185fe 6%, var(--surface));
      border-right: 1px solid color-mix(in oklch, #1185fe 15%, var(--border));
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 3px;
      position: relative; z-index: 2;
    }
    .b4-bar {
      width: 8px; height: 2px;
      background: #1185fe; opacity: 0.3;
    }
    .b4-bar:nth-child(2) { opacity: 0.5; }
    .b4-bar:last-child { opacity: 0.8; }
    .b4-body {
      flex: 1;
      padding: var(--sp-2) var(--sp-3);
      display: flex;
      flex-direction: column;
      gap: var(--sp);
      position: relative; z-index: 2;
    }
    .b4-rx {
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: #1185fe;
      letter-spacing: 0.1em;
    }

    /* --- BX: Signal Ping Hybrid (Packet header + Chevron + Rings) --- */
    .bx-hybrid {
      position: relative;
      overflow: hidden;
      background: var(--surface-2);
      /* Chevron right edge + stepped TL corner */
      clip-path: polygon(
        4px 0, 4px 4px, 0 4px,
        0 100%,
        calc(100% - 14px) 100%,
        100% 50%,
        calc(100% - 14px) 0
      );
      padding: 0;
      display: flex;
      flex-direction: column;
      border-left: 1px dashed color-mix(in oklch, #1185fe 30%, var(--border));
      border-top: 1px solid color-mix(in oklch, #1185fe 30%, var(--border));
      border-bottom: 1px solid color-mix(in oklch, #1185fe 30%, var(--border));
    }
    /* Concentric rings from chevron point — bold enough to actually see */
    .bx-hybrid::before {
      content: '';
      position: absolute; inset: 0;
      background: radial-gradient(circle at 92% 50%,
        transparent 18px, color-mix(in oklch, #1185fe 22%, transparent) 19px, color-mix(in oklch, #1185fe 22%, transparent) 21px, transparent 22px,
        transparent 38px, color-mix(in oklch, #1185fe 15%, transparent) 39px, color-mix(in oklch, #1185fe 15%, transparent) 41px, transparent 42px,
        transparent 58px, color-mix(in oklch, #1185fe 10%, transparent) 59px, color-mix(in oklch, #1185fe 10%, transparent) 61px, transparent 62px,
        transparent 78px, color-mix(in oklch, #1185fe 6%, transparent) 79px, color-mix(in oklch, #1185fe 6%, transparent) 81px, transparent 82px
      );
      pointer-events: none; z-index: 1;
    }
    .bx-header {
      display: flex;
      padding: var(--sp) var(--sp-2);
      padding-left: calc(var(--sp-2) + 4px);
      gap: var(--sp-2);
      background: color-mix(in oklch, #1185fe 4%, var(--surface));
      border-bottom: 1px solid color-mix(in oklch, #1185fe 15%, var(--border));
      flex-shrink: 0;
      font-family: var(--mono);
      font-size: var(--fs-s);
      position: relative; z-index: 2;
    }
    .bx-field {
      color: var(--fg-2);
      border-right: 1px solid color-mix(in oklch, #1185fe 15%, var(--border));
      padding-right: var(--sp-2);
    }
    .bx-field:last-child { border-right: none; padding-right: 20px; }
    .bx-field em { color: #1185fe; font-style: normal; }
    .bx-body {
      flex: 1;
      padding: var(--sp-2) var(--sp-3);
      padding-right: 24px;
      display: flex;
      flex-direction: column;
      gap: var(--sp);
      position: relative; z-index: 2;
    }
    .bx-footer {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: var(--sp) var(--sp-2);
      padding-right: 24px;
      background: color-mix(in oklch, #1185fe 3%, var(--surface));
      border-top: 1px solid color-mix(in oklch, #1185fe 12%, var(--border));
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2);
      flex-shrink: 0;
      position: relative; z-index: 2;
    }
    .bx-bars {
      display: flex; gap: 2px; align-items: flex-end;
    }
    .bx-bars i {
      display: block; width: 3px; background: #1185fe;
    }
    .bx-bars i:nth-child(1) { height: 4px; opacity: 0.4; }
    .bx-bars i:nth-child(2) { height: 7px; opacity: 0.6; }
    .bx-bars i:nth-child(3) { height: 10px; opacity: 0.9; }

    /* --- B5: Torn Fragment --- */
    .b5-torn {
      position: relative;
      overflow: hidden;
      clip-path: polygon(
        0 5px, 3% 1px, 6% 6px, 10% 2px, 14% 5px, 18% 0, 22% 4px, 27% 1px,
        32% 6px, 37% 2px, 42% 5px, 47% 0, 52% 4px, 57% 1px, 62% 5px, 67% 2px,
        72% 6px, 77% 1px, 82% 4px, 87% 0, 92% 5px, 96% 2px, 100% 4px,
        100% 100%, 0 100%
      );
      background: color-mix(in oklch, var(--surface-2) 90%, rgba(255,255,255,0.03));
      padding: 14px var(--sp-3) var(--sp-3);
      display: flex;
      flex-direction: column;
      gap: var(--sp);
      filter: drop-shadow(0 -2px 3px rgba(0,0,0,0.12));
      transition: transform 0.2s ease;
    }
    .b5-torn:hover { transform: rotate(-0.3deg) translateY(-2px); }
    .b5-torn::before {
      content: '';
      position: absolute; inset: 0;
      background:
        linear-gradient(0deg, transparent 12px, rgba(255,255,255,0.06) 12px, rgba(255,255,255,0.06) 13px, transparent 13px),
        linear-gradient(0deg, transparent 37px, rgba(255,255,255,0.05) 37px, rgba(255,255,255,0.05) 38px, transparent 38px),
        linear-gradient(0deg, transparent 68px, rgba(255,255,255,0.04) 68px, rgba(255,255,255,0.04) 69px, transparent 69px);
      pointer-events: none;
    }
    .b5-stripe {
      height: 2px;
      background: #1185fe;
      margin-top: auto;
    }
    .b5-tag {
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: #1185fe;
    }

    /* ========================================
       LINK VARIANT C — Intercept
       ======================================== */

    .lC-intercept {
      position: relative;
      overflow: hidden;
      background: var(--bg);
      padding: 0;
      display: flex;
      flex-direction: column;
      color: var(--fg);
      grid-column: span 2;
      /* Notched top edge — torn-off teletype */
      clip-path: polygon(
        0 6px, 4% 2px, 8% 7px, 13% 3px, 18% 6px, 23% 1px, 28% 5px,
        33% 2px, 38% 7px, 43% 3px, 48% 6px, 53% 1px, 58% 5px,
        63% 2px, 68% 6px, 73% 3px, 78% 7px, 83% 2px, 88% 5px,
        93% 1px, 97% 6px, 100% 3px,
        100% 100%,
        calc(100% - 10px) 100%, calc(100% - 10px) calc(100% - 10px), 0 calc(100% - 10px)
      );
      border: 1px solid color-mix(in oklch, var(--link-accent) 35%, var(--border));
      filter: drop-shadow(0 -1px 2px rgba(0,0,0,0.1));
    }
    .lC-intercept:hover {
      border-color: var(--link-accent);
    }
    /* Horizontal ruled-paper lines */
    .lC-intercept::before {
      content: '';
      position: absolute; inset: 0;
      background: repeating-linear-gradient(
        0deg,
        transparent, transparent 22px,
        color-mix(in oklch, var(--link-accent) 6%, transparent) 22px,
        color-mix(in oklch, var(--link-accent) 6%, transparent) 23px
      );
      pointer-events: none; z-index: 1;
    }
    .lC-stamp {
      position: absolute;
      top: 14px; right: 12px;
      font-family: var(--mono);
      font-size: 9px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.2em;
      color: var(--link-accent);
      opacity: 0.25;
      transform: rotate(4deg);
      border: 1px solid color-mix(in oklch, var(--link-accent) 20%, transparent);
      padding: 1px 6px;
      z-index: 2;
    }
    .lC-head {
      display: flex;
      align-items: baseline;
      gap: var(--sp-2);
      padding: 12px var(--sp-3) var(--sp);
      flex-shrink: 0;
      position: relative; z-index: 2;
    }
    .lC-arrow {
      color: var(--link-accent);
      font-weight: 700;
      font-size: var(--fs);
    }
    .lC-domain {
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--link-accent);
      opacity: 0.8;
    }
    .lC-date {
      margin-left: auto;
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2);
    }
    .lC-body {
      flex: 1;
      padding: 0 var(--sp-3) var(--sp-3);
      display: flex;
      flex-direction: column;
      gap: var(--sp);
      position: relative; z-index: 2;
    }
    .lC-body h3 {
      font-size: var(--fs);
      font-weight: 600;
      text-transform: none;
      letter-spacing: 0;
      line-height: var(--lh);
    }
    .lC-comment {
      font-size: var(--fs-s);
      color: var(--fg-2);
      line-height: var(--lh);
      font-style: italic;
    }

    /* ========================================
       LINK VARIANT D — Beacon
       ======================================== */

    .lD-beacon {
      position: relative;
      overflow: hidden;
      background: var(--bg);
      padding: 0;
      display: flex;
      color: var(--fg);
      grid-column: span 2;
      /* Right-pointing chevron */
      clip-path: polygon(
        0 0, calc(100% - 20px) 0,
        100% 50%,
        calc(100% - 20px) 100%,
        0 100%
      );
      border: 1px solid color-mix(in oklch, var(--link-accent) 35%, var(--border));
    }
    .lD-beacon:hover {
      border-color: var(--link-accent);
    }
    /* Radar rings from right chevron tip */
    .lD-beacon::before {
      content: '';
      position: absolute; inset: 0;
      background: radial-gradient(circle at 95% 50%,
        transparent 12px, color-mix(in oklch, var(--link-accent) 15%, transparent) 13px, color-mix(in oklch, var(--link-accent) 15%, transparent) 15px, transparent 16px,
        transparent 30px, color-mix(in oklch, var(--link-accent) 10%, transparent) 31px, color-mix(in oklch, var(--link-accent) 10%, transparent) 33px, transparent 34px,
        transparent 50px, color-mix(in oklch, var(--link-accent) 6%, transparent) 51px, color-mix(in oklch, var(--link-accent) 6%, transparent) 53px, transparent 54px,
        transparent 72px, color-mix(in oklch, var(--link-accent) 4%, transparent) 73px, color-mix(in oklch, var(--link-accent) 4%, transparent) 75px, transparent 76px
      );
      pointer-events: none; z-index: 1;
    }
    /* Accent sidebar */
    .lD-side {
      width: 3px;
      flex-shrink: 0;
      background: linear-gradient(180deg, var(--link-accent), color-mix(in oklch, var(--link-accent) 20%, transparent));
    }
    .lD-content {
      flex: 1;
      padding: var(--sp-2) var(--sp-3);
      padding-right: 28px;
      display: flex;
      flex-direction: column;
      gap: var(--sp);
      position: relative; z-index: 2;
    }
    .lD-tag {
      font-family: var(--mono);
      font-size: var(--fs-s);
      text-transform: uppercase;
      letter-spacing: 0.12em;
      color: var(--link-accent);
    }
    .lD-content h3 {
      font-size: var(--fs);
      font-weight: 600;
      text-transform: none;
      letter-spacing: 0;
      line-height: var(--lh);
    }
    .lD-meta {
      display: flex;
      gap: var(--sp-2);
      align-items: baseline;
      margin-top: auto;
    }
    .lD-domain {
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--link-accent);
      opacity: 0.8;
    }
    .lD-date {
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2);
      margin-left: auto;
    }

    /* ========================================
       LINK VARIANT E — Uplink
       ======================================== */

    .lE-uplink {
      position: relative;
      overflow: hidden;
      background: var(--bg);
      padding: 0;
      display: flex;
      flex-direction: column;
      color: var(--fg);
      grid-column: span 2;
      /* Angled satellite-dish cut on top-left */
      clip-path: polygon(
        36px 0, 100% 0,
        100% 100%,
        0 100%,
        0 28px
      );
      border: 1px solid color-mix(in oklch, var(--link-accent) 35%, var(--border));
    }
    .lE-uplink:hover {
      border-color: var(--link-accent);
    }
    /* Frequency wave lines */
    .lE-uplink::before {
      content: '';
      position: absolute; inset: 0;
      background:
        linear-gradient(135deg, transparent 18px, color-mix(in oklch, var(--link-accent) 10%, transparent) 18.5px, color-mix(in oklch, var(--link-accent) 10%, transparent) 19px, transparent 19.5px),
        linear-gradient(135deg, transparent 28px, color-mix(in oklch, var(--link-accent) 6%, transparent) 28.5px, color-mix(in oklch, var(--link-accent) 6%, transparent) 29px, transparent 29.5px);
      pointer-events: none; z-index: 1;
    }
    .lE-freq {
      display: flex;
      align-items: center;
      gap: var(--sp-2);
      padding: var(--sp) var(--sp-3);
      padding-left: 44px;
      background: color-mix(in oklch, var(--link-accent) 5%, var(--surface-2));
      border-bottom: 1px solid color-mix(in oklch, var(--link-accent) 18%, var(--border));
      flex-shrink: 0;
      position: relative; z-index: 2;
    }
    .lE-arrow {
      color: var(--link-accent);
      font-weight: 700;
      font-size: var(--fs);
    }
    .lE-label {
      font-family: var(--mono);
      font-size: var(--fs-s);
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--link-accent);
    }
    .lE-bars {
      margin-left: auto;
      display: flex;
      gap: 2px;
      align-items: flex-end;
    }
    .lE-bars i {
      display: block;
      width: 3px;
      background: var(--link-accent);
    }
    .lE-bars i:nth-child(1) { height: 4px; opacity: 0.3; }
    .lE-bars i:nth-child(2) { height: 6px; opacity: 0.5; }
    .lE-bars i:nth-child(3) { height: 8px; opacity: 0.7; }
    .lE-bars i:nth-child(4) { height: 10px; opacity: 1; }
    .lE-body {
      flex: 1;
      padding: var(--sp-2) var(--sp-3);
      display: flex;
      flex-direction: column;
      gap: var(--sp);
      position: relative; z-index: 2;
    }
    .lE-body h3 {
      font-size: var(--fs);
      font-weight: 600;
      text-transform: none;
      letter-spacing: 0;
      line-height: var(--lh);
    }
    .lE-comment {
      font-size: var(--fs-s);
      color: var(--fg-2);
      line-height: var(--lh);
      font-style: italic;
    }
    .lE-foot {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      padding: var(--sp) var(--sp-3);
      background: color-mix(in oklch, var(--link-accent) 3%, var(--surface-2));
      border-top: 1px solid color-mix(in oklch, var(--link-accent) 12%, var(--border));
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2);
      flex-shrink: 0;
      position: relative; z-index: 2;
    }
    .lE-domain {
      color: var(--link-accent);
      opacity: 0.8;
    }

    /* ========================================
       Shared helpers
       ======================================== */
    .tile-title {
      font-size: var(--fs); font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      line-height: var(--lh);
      color: var(--fg);
    }
    .tile-subtitle {
      font-size: var(--fs-s);
      color: var(--fg-2);
      line-height: var(--lh);
    }
    .tile-time {
      font-family: var(--mono);
      font-size: var(--fs-s);
      color: var(--fg-2);
    }
    .tile-accent {
      font-family: var(--mono);
      font-size: var(--fs-s);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--accent);
    }
  </style>

  <section class="demo-grid">
    <!-- === POST DIRECTIONS === -->
    <div class="demo-section">
      <h2>Post Tiles — 4 Directions</h2>
      <p>Dossier, Circuit Board, Blast Door, Cassette (original + circuit hybrid)</p>
    </div>

    <!-- P1: Dossier Folder -->
    <div class="p1-dossier">
      <span class="p1-stamp">07</span>
      <span class="p1-tab">classified</span>
      <span class="tile-title">Dossier Folder</span>
      <span class="tile-subtitle">Tab protrusion, dog-ear, ruled paper</span>
      <span class="tile-time">2025-03-15</span>
    </div>

    <div class="p1-dossier demo-w">
      <span class="p1-stamp">12</span>
      <span class="p1-tab">classified</span>
      <span class="tile-title">Building UI in GPUI</span>
      <span class="tile-subtitle">A deep look at the rendering framework behind Zed, exploring how GPUI handles retained-mode rendering and element layout</span>
      <span class="tile-time">2024-08-20</span>
    </div>

    <!-- P2: Circuit Board -->
    <div class="p2-circuit">
      <div class="p2-chip">
        <span class="p2-part">IC-07</span>
        <span class="tile-title">Circuit Board</span>
      </div>
    </div>

    <div class="p2-circuit demo-w">
      <div class="p2-chip">
        <span class="p2-part">IC-12 REV.A</span>
        <span class="tile-title">Building UI in GPUI</span>
        <span class="tile-subtitle">Pin notches on sides, PCB grid, trace lines</span>
      </div>
    </div>

    <!-- P3: Blast Door -->
    <div class="p3-blast">
      <div class="p3-rail">
        <div class="p3-hex"></div>
        <div class="p3-hex"></div>
        <span class="p3-label">Post</span>
        <div class="p3-hex"></div>
      </div>
      <div class="p3-body">
        <span class="tile-title">Blast Door</span>
        <span class="tile-subtitle">Heavy top bevel, hex bolts, warning stripe</span>
        <span class="tile-time">2025-03-15</span>
      </div>
      <div class="p3-stripe"></div>
    </div>

    <div class="p3-blast demo-w">
      <div class="p3-rail">
        <div class="p3-hex"></div>
        <div class="p3-hex"></div>
        <div class="p3-hex"></div>
        <span class="p3-label">Post</span>
        <div class="p3-hex"></div>
      </div>
      <div class="p3-body">
        <span class="tile-title">Building UI in GPUI</span>
        <span class="tile-subtitle">The rendering framework behind Zed, retained-mode rendering and element layout</span>
        <span class="tile-time">2024-08-20</span>
      </div>
      <div class="p3-stripe"></div>
    </div>

    <!-- P4: Original Cassette (for comparison) -->
    <div class="p4-cassette">
      <span class="p4-catalog">NR-07</span>
      <div class="p4-label">
        <span class="tile-accent">post</span>
        <span class="tile-title">Cassette (original)</span>
        <span class="tile-time">2025-03-15</span>
      </div>
      <div class="p4-window"></div>
    </div>

    <!-- PX: Cassette + Circuit Hybrid -->
    <div class="px-hybrid">
      <span class="px-catalog">NR-07</span>
      <div class="px-label">
        <span class="tile-accent">post</span>
        <span class="tile-title">Cassette + Circuit</span>
        <span class="tile-time">2025-03-15</span>
      </div>
      <div class="px-edge"></div>
    </div>

    <div class="px-hybrid demo-w">
      <span class="px-catalog">NR-12</span>
      <div class="px-label">
        <span class="tile-accent">post</span>
        <span class="tile-title">Building UI in GPUI</span>
        <span class="tile-subtitle">Cassette body + circuit board insertion edge with PCB grid, traces, and pin notches</span>
        <span class="tile-time">2024-08-20</span>
      </div>
      <div class="px-edge"></div>
    </div>

    <div class="px-hybrid demo-w demo-tall">
      <span class="px-catalog">NR-12</span>
      <div class="px-label">
        <span class="tile-accent">post</span>
        <span class="tile-title">Building UI in GPUI</span>
        <span class="tile-subtitle">A deep look at the rendering framework behind Zed, exploring how GPUI handles retained-mode rendering and element layout in a code editor. The cassette body holds the content while the circuit edge provides the insertion interface.</span>
        <span class="tile-time">2024-08-20</span>
      </div>
      <div class="px-edge"></div>
    </div>

    <!-- === TYPOGRAPHIC POST DIRECTIONS === -->
    <div class="demo-section">
      <h2>Post Tiles — Typographic Focus</h2>
      <p>Slab Monument, Redacted Dossier, Departure Board</p>
    </div>

    <!-- P8: Cassette + Slab hybrid — cassette shell, giant title -->
    <div class="p8-casslab">
      <span class="p8-catalog">NR-07</span>
      <div class="p8-label">
        <span class="p8-big">GPUI</span>
      </div>
      <div class="p8-footer">
        <span class="p8-type">post</span>
        <span class="p8-date">2025-03-15</span>
      </div>
      <div class="p8-edge"></div>
    </div>

    <div class="p8-casslab demo-w">
      <span class="p8-catalog">NR-12</span>
      <div class="p8-label">
        <span class="p8-big">Building UI in GPUI</span>
      </div>
      <div class="p8-footer">
        <span class="p8-type">post</span>
        <span class="p8-date">2024-08-20</span>
      </div>
      <div class="p8-edge"></div>
    </div>

    <div class="p8-casslab demo-w demo-tall">
      <span class="p8-catalog">NR-12</span>
      <div class="p8-label">
        <span class="p8-big">Building UI in GPUI</span>
        <span class="p8-sub">A deep look at the rendering framework behind Zed, exploring how GPUI handles retained-mode rendering and element layout</span>
      </div>
      <div class="p8-footer">
        <span class="p8-type">post</span>
        <span class="p8-date">2024-08-20</span>
      </div>
      <div class="p8-edge"></div>
    </div>

    <!-- Another short title at 1x1 -->
    <div class="p8-casslab">
      <span class="p8-catalog">NR-03</span>
      <div class="p8-label">
        <span class="p8-big">On Craft</span>
      </div>
      <div class="p8-footer">
        <span class="p8-type">post</span>
        <span class="p8-date">2024-06-22</span>
      </div>
      <div class="p8-edge"></div>
    </div>

    <!-- Long title at 1x1 — tests clipping -->
    <div class="p8-casslab">
      <span class="p8-catalog">NR-11</span>
      <div class="p8-label">
        <span class="p8-big">Design Tokens at Scale</span>
      </div>
      <div class="p8-footer">
        <span class="p8-type">post</span>
        <span class="p8-date">2024-07-15</span>
      </div>
      <div class="p8-edge"></div>
    </div>

    <!-- P7: Departure Board — split-flap / arrivals board -->
    <div class="p7-board">
      <div class="p7-row p7-row-head">
        <span class="p7-col p7-col-id">ID</span>
        <span class="p7-col p7-col-dest">TITLE</span>
        <span class="p7-col p7-col-time">DATE</span>
      </div>
      <div class="p7-row p7-row-active">
        <span class="p7-col p7-col-id">07</span>
        <span class="p7-col p7-col-dest">DEPARTURE BOARD</span>
        <span class="p7-col p7-col-time">03-15</span>
      </div>
      <div class="p7-row p7-row-dim">
        <span class="p7-col p7-col-id">06</span>
        <span class="p7-col p7-col-dest">PREVIOUS ENTRY</span>
        <span class="p7-col p7-col-time">03-01</span>
      </div>
    </div>

    <div class="p7-board demo-w">
      <div class="p7-row p7-row-head">
        <span class="p7-col p7-col-id">ID</span>
        <span class="p7-col p7-col-dest">TITLE</span>
        <span class="p7-col p7-col-time">DATE</span>
        <span class="p7-col p7-col-status">STATUS</span>
      </div>
      <div class="p7-row p7-row-active">
        <span class="p7-col p7-col-id">12</span>
        <span class="p7-col p7-col-dest">BUILDING UI IN GPUI</span>
        <span class="p7-col p7-col-time">08-20</span>
        <span class="p7-col p7-col-status p7-live">LIVE</span>
      </div>
      <div class="p7-row p7-row-dim">
        <span class="p7-col p7-col-id">11</span>
        <span class="p7-col p7-col-dest">DESIGN TOKENS AT SCALE</span>
        <span class="p7-col p7-col-time">07-15</span>
        <span class="p7-col p7-col-status">ARCH</span>
      </div>
      <div class="p7-row p7-row-dim">
        <span class="p7-col p7-col-id">10</span>
        <span class="p7-col p7-col-dest">THOUGHTS ON CRAFT</span>
        <span class="p7-col p7-col-time">06-22</span>
        <span class="p7-col p7-col-status">ARCH</span>
      </div>
    </div>

    <div class="p7-board demo-w demo-tall">
      <div class="p7-row p7-row-head">
        <span class="p7-col p7-col-id">ID</span>
        <span class="p7-col p7-col-dest">TITLE</span>
        <span class="p7-col p7-col-time">DATE</span>
        <span class="p7-col p7-col-status">STATUS</span>
      </div>
      <div class="p7-row p7-row-active">
        <span class="p7-col p7-col-id">12</span>
        <span class="p7-col p7-col-dest">BUILDING UI IN GPUI</span>
        <span class="p7-col p7-col-time">08-20</span>
        <span class="p7-col p7-col-status p7-live">LIVE</span>
      </div>
      <div class="p7-row">
        <span class="p7-col p7-col-id">11</span>
        <span class="p7-col p7-col-dest">DESIGN TOKENS AT SCALE</span>
        <span class="p7-col p7-col-time">07-15</span>
        <span class="p7-col p7-col-status">ARCH</span>
      </div>
      <div class="p7-row">
        <span class="p7-col p7-col-id">10</span>
        <span class="p7-col p7-col-dest">THOUGHTS ON CRAFT</span>
        <span class="p7-col p7-col-time">06-22</span>
        <span class="p7-col p7-col-status">ARCH</span>
      </div>
      <div class="p7-row p7-row-dim">
        <span class="p7-col p7-col-id">09</span>
        <span class="p7-col p7-col-dest">SHIPPING INCREMENTALLY</span>
        <span class="p7-col p7-col-time">05-10</span>
        <span class="p7-col p7-col-status">ARCH</span>
      </div>
      <div class="p7-row p7-row-dim">
        <span class="p7-col p7-col-id">08</span>
        <span class="p7-col p7-col-dest">ON VISUAL DENSITY</span>
        <span class="p7-col p7-col-time">04-03</span>
        <span class="p7-col p7-col-status">ARCH</span>
      </div>
    </div>

    <!-- === SHOT DIRECTIONS (3 variants) === -->
    <div class="demo-section">
      <h2>Shot Tiles — 3 Rotating Variants</h2>
      <p>Viewfinder, Evidence Photo, HUD Capture — alternate based on index/size</p>
    </div>

    <!-- S1: Viewfinder Frame -->
    <div class="s1-viewfinder demo-w">
      <div class="shot-img"></div>
      <span class="s1-exposure">f/42 &bull; 2025-01-10</span>
      <div class="s1-ticks"></div>
    </div>

    <!-- S2: Evidence Photo -->
    <div class="s2-evidence demo-w">
      <div class="shot-img"></div>
      <div class="s2-marker">42</div>
    </div>

    <!-- S4: HUD Capture -->
    <div class="s4-hud demo-w">
      <div class="shot-img"></div>
      <div class="s4-data">REC<br/>042</div>
    </div>

    <!-- Show them at 1x1 size too -->
    <div class="s1-viewfinder">
      <div class="shot-img"></div>
      <span class="s1-exposure">f/15</span>
    </div>

    <div class="s2-evidence">
      <div class="shot-img"></div>
      <div class="s2-marker">15</div>
    </div>

    <div class="s4-hud">
      <div class="shot-img"></div>
      <div class="s4-data">015</div>
    </div>

    <!-- === BSKY DIRECTIONS === -->
    <div class="demo-section">
      <h2>Bsky Tiles — Signal Packet, Comm Ping, Hybrid, Torn</h2>
      <p>Protocol-inspired: structured headers, chevron shapes, concentric rings</p>
    </div>

    <!-- B2: Signal Packet -->
    <div class="b2-packet demo-w">
      <div class="b2-header">
        <span class="b2-field"><em>SRC</em> nate.rip</span>
        <span class="b2-field"><em>TS</em> 2d ago</span>
        <span class="b2-field"><em>LEN</em> 142</span>
      </div>
      <div class="b2-payload">
        <span class="bsky-text-demo">Signal Packet — Stepped stamp corners, structured header with field dividers, dashed side borders, hex checksum</span>
      </div>
      <div class="b2-checksum">0x0F likes &bull; 0x03 rpl</div>
    </div>

    <!-- B4: Comm Ping -->
    <div class="b4-ping demo-w">
      <div class="b4-telem">
        <div class="b4-bar"></div>
        <div class="b4-bar"></div>
        <div class="b4-bar"></div>
      </div>
      <div class="b4-body">
        <span class="b4-rx">RX</span>
        <span class="bsky-text-demo">Comm Ping — Right-edge chevron, concentric rings from point, telemetry sidebar</span>
        <span class="tile-time">3d ago</span>
      </div>
    </div>

    <!-- BX: Signal Ping Hybrid -->
    <div class="bx-hybrid demo-w">
      <div class="bx-header">
        <span class="bx-field"><em>SRC</em> nate.rip</span>
        <span class="bx-field"><em>TS</em> 2d ago</span>
        <span class="bx-field"><em>LEN</em> 142</span>
      </div>
      <div class="bx-body">
        <span class="bsky-text-demo">Signal Ping Hybrid — Chevron shape + packet header with field dividers + concentric rings from the arrow point + hex engagement footer</span>
      </div>
      <div class="bx-footer">
        <span>0x0F &bull; 0x03 rpl</span>
        <div class="bx-bars"><i></i><i></i><i></i></div>
      </div>
    </div>

    <!-- BX at 1x1 -->
    <div class="bx-hybrid">
      <div class="bx-header">
        <span class="bx-field"><em>SRC</em> nate.rip</span>
        <span class="bx-field"><em>TS</em> 5d</span>
      </div>
      <div class="bx-body">
        <span class="bsky-text-demo">Hybrid at 1x1 — compact but still has the chevron + header</span>
      </div>
      <div class="bx-footer">
        <span>0x03</span>
        <div class="bx-bars"><i></i><i></i><i></i></div>
      </div>
    </div>

    <!-- B5: Torn Fragment -->
    <div class="b5-torn demo-w">
      <span class="b5-tag">bsky</span>
      <span class="bsky-text-demo">Torn Fragment — Jagged ragged top edge, paper texture, drop shadow, rotation on hover. The organic counterpoint.</span>
      <span class="tile-time">5d ago</span>
      <div class="b5-stripe"></div>
    </div>

    <div class="b5-torn">
      <span class="b5-tag">bsky</span>
      <span class="bsky-text-demo">Torn at 1x1 — still reads as ripped from a feed</span>
      <div class="b5-stripe"></div>
    </div>

    <!-- === LINK VARIANTS === -->
    <div class="demo-section">
      <h2>Link Tiles — 5 Relay-Family Variants</h2>
      <p>A: Relay (diagonal chamfer), B: Dispatch (stepped), C: Intercept (torn top), D: Beacon (chevron), E: Uplink (satellite cut)</p>
    </div>

    <!-- Relay A (current production style, simplified for demo) -->
    <div style="grid-column: span 2; position: relative; overflow: hidden; background: var(--bg); padding: 0; display: flex; flex-direction: column; clip-path: polygon(14px 0, 100% 0, 100% calc(100% - 18px), calc(100% - 18px) 100%, 0 100%, 0 14px); border: 2px solid color-mix(in oklch, var(--link-accent) 50%, var(--border)); border-top: none;">
      <div style="display: flex; align-items: center; gap: var(--sp-2); padding: var(--sp) var(--sp-3); padding-left: calc(var(--sp-3) + 8px); background: color-mix(in oklch, var(--link-accent) 8%, var(--surface-2)); border-bottom: 1px solid color-mix(in oklch, var(--link-accent) 25%, var(--border)); font-family: var(--mono); font-size: var(--fs-s);">
        <span style="font-size: var(--fs-l); font-weight: 700; color: var(--link-accent); line-height: 1;">↗</span>
        <span style="text-transform: uppercase; letter-spacing: 0.12em; color: var(--link-accent);">relay</span>
        <span style="margin-left: auto; color: var(--link-accent); opacity: 0.8;">daniloleal.co</span>
      </div>
      <div style="flex: 1; padding: var(--sp-3); display: flex; flex-direction: column; gap: var(--sp);">
        <span class="tile-title" style="text-transform: none; letter-spacing: 0;">The Surprising Complexity</span>
        <span class="tile-subtitle" style="font-style: italic;">Danilo explores the decisions and details</span>
      </div>
      <div style="display: flex; justify-content: space-between; padding: var(--sp) var(--sp-3); background: color-mix(in oklch, var(--link-accent) 5%, var(--surface-2)); border-top: 1px solid color-mix(in oklch, var(--link-accent) 18%, var(--border)); font-family: var(--mono); font-size: var(--fs-s); color: var(--fg-2);">
        <span>2024-09-11</span>
      </div>
    </div>

    <!-- Dispatch B -->
    <div class="lB-dispatch">
      <div class="lB-stripe"></div>
      <div class="lB-head">
        <span class="lB-arrow">↗</span>
        <span class="lB-domain">x.com</span>
        <span class="lB-date">2024-08-20</span>
      </div>
      <div class="lB-body">
        <h3>Launching Zed's agent panel, inline assist and more</h3>
        <span class="lB-comment">Launching Zed's agent panel, inline assist and more</span>
      </div>
    </div>

    <!-- Side by side: A then B -->
    <div style="grid-column: span 2; position: relative; overflow: hidden; background: var(--bg); padding: 0; display: flex; flex-direction: column; clip-path: polygon(14px 0, 100% 0, 100% calc(100% - 18px), calc(100% - 18px) 100%, 0 100%, 0 14px); border: 2px solid color-mix(in oklch, var(--link-accent) 50%, var(--border)); border-top: none;">
      <div style="display: flex; align-items: center; gap: var(--sp-2); padding: var(--sp) var(--sp-3); padding-left: calc(var(--sp-3) + 8px); background: color-mix(in oklch, var(--link-accent) 8%, var(--surface-2)); border-bottom: 1px solid color-mix(in oklch, var(--link-accent) 25%, var(--border)); font-family: var(--mono); font-size: var(--fs-s);">
        <span style="font-size: var(--fs-l); font-weight: 700; color: var(--link-accent); line-height: 1;">↗</span>
        <span style="text-transform: uppercase; letter-spacing: 0.12em; color: var(--link-accent);">relay</span>
        <span style="margin-left: auto; color: var(--link-accent); opacity: 0.8;">spotify.com</span>
      </div>
      <div style="flex: 1; padding: var(--sp-3); display: flex; flex-direction: column; gap: var(--sp);">
        <span class="tile-title" style="text-transform: none; letter-spacing: 0;">I did a podcast?</span>
      </div>
      <div style="display: flex; justify-content: space-between; padding: var(--sp) var(--sp-3); background: color-mix(in oklch, var(--link-accent) 5%, var(--surface-2)); border-top: 1px solid color-mix(in oklch, var(--link-accent) 18%, var(--border)); font-family: var(--mono); font-size: var(--fs-s); color: var(--fg-2);">
        <span>2024-11-24</span>
      </div>
    </div>

    <div class="lB-dispatch">
      <div class="lB-stripe"></div>
      <div class="lB-head">
        <span class="lB-arrow">↗</span>
        <span class="lB-domain">maggieappleton.com</span>
        <span class="lB-date">2025-07-09</span>
      </div>
      <div class="lB-body">
        <h3>Gastown — Local-first software design</h3>
      </div>
    </div>

    <!-- Intercept C — torn top teletype -->
    <div class="lC-intercept">
      <span class="lC-stamp">intercepted</span>
      <div class="lC-head">
        <span class="lC-arrow">↗</span>
        <span class="lC-domain">daniloleal.co</span>
        <span class="lC-date">2024-09-11</span>
      </div>
      <div class="lC-body">
        <h3>The Surprising Complexity of Design Systems</h3>
        <span class="lC-comment">Danilo explores the decisions and details behind scaling a design system</span>
      </div>
    </div>

    <div class="lC-intercept">
      <span class="lC-stamp">intercepted</span>
      <div class="lC-head">
        <span class="lC-arrow">↗</span>
        <span class="lC-domain">x.com</span>
        <span class="lC-date">2024-08-20</span>
      </div>
      <div class="lC-body">
        <h3>Launching Zed's agent panel</h3>
      </div>
    </div>

    <!-- Beacon D — chevron with radar rings -->
    <div class="lD-beacon">
      <div class="lD-side"></div>
      <div class="lD-content">
        <span class="lD-tag">beacon</span>
        <h3>Maggie Appleton — Gastown: Local-first Software Design</h3>
        <div class="lD-meta">
          <span class="lD-domain">maggieappleton.com</span>
          <span class="lD-date">2025-07-09</span>
        </div>
      </div>
    </div>

    <div class="lD-beacon">
      <div class="lD-side"></div>
      <div class="lD-content">
        <span class="lD-tag">beacon</span>
        <h3>I did a podcast?</h3>
        <div class="lD-meta">
          <span class="lD-domain">spotify.com</span>
          <span class="lD-date">2024-11-24</span>
        </div>
      </div>
    </div>

    <!-- Uplink E — satellite-dish angle -->
    <div class="lE-uplink">
      <div class="lE-freq">
        <span class="lE-arrow">↗</span>
        <span class="lE-label">uplink</span>
        <div class="lE-bars"><i></i><i></i><i></i><i></i></div>
      </div>
      <div class="lE-body">
        <h3>The Surprising Complexity of Design Systems</h3>
        <span class="lE-comment">A thoughtful exploration of tradeoffs at scale</span>
      </div>
      <div class="lE-foot">
        <span class="lE-domain">daniloleal.co</span>
        <span>2024-09-11</span>
      </div>
    </div>

    <div class="lE-uplink">
      <div class="lE-freq">
        <span class="lE-arrow">↗</span>
        <span class="lE-label">uplink</span>
        <div class="lE-bars"><i></i><i></i><i></i><i></i></div>
      </div>
      <div class="lE-body">
        <h3>Gastown — Local-first software design</h3>
      </div>
      <div class="lE-foot">
        <span class="lE-domain">maggieappleton.com</span>
        <span>2025-07-09</span>
      </div>
    </div>

  </section>
</Base>
