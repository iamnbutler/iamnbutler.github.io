---
import { Image } from "astro:assets";

interface Props {
    className?: string;
}

const {
    className = "top-[-96px] left-[40px] z-[1] lg:top-[-120px] lg:left-[60px] pointer-events-none absolute rotate-12 opacity-10 lg:opacity-100",
} = Astro.props;
---

<Image
    width="192"
    height="192"
    class={className}
    src="/old_zed_sticker.png"
    alt=""
/>
